extends layout

block content
    p Info about: #{visitor.name}
    p Age: #{visitor.age}
    p Number of orders: #{orders.length}

    each order, index in orders  
        li Order #{index + 1} : #{order}

    //- - var orders = visitor.orders[0]

    //- if orders
    //-     p Number of orders : 0
    //- else
    //-     p Number of orders : #{orders}
   
    
       
    
